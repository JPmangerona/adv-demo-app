meta {
  name: 1 - GET processos SEM token (403)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/processos
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:6000
}

tests {
  test("Status 403 - Acesso negado sem token", function() {
    expect(res.status).to.equal(403);
  });
}
