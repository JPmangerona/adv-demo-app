meta {
  name: 2 - GET clientes SEM token (403)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/clientes
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:6000
}

tests {
  test("Status 403 - Acesso negado sem token", function() {
    expect(res.status).to.equal(403);
  });
}
