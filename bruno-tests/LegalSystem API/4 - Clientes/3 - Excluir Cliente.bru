meta {
  name: 3 - Excluir Cliente
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/clientes/{{clienteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAdvogado}}
}

vars:pre-request {
  baseUrl: http://localhost:6000
}

tests {
  test("Status 200 - Excluido", function() {
    expect(res.status).to.equal(200);
  });
}
